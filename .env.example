# Copy to .env and fill in. .env is git-ignored.
#
# Needed by the raw Claude API exercises: domains 2, 4, 5.
# NOT needed by the Agent SDK exercises (domains 1, 3) — those use your existing
# Claude Code login.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: run the whole lab against a cheaper model while iterating.
# CCAR_MODEL=claude-haiku-4-5
